# Dashboard Module Roadmap

This folder is reserved for the analytics UI layer (Streamlit recommended).

Planned MVP pages:

- data quality summary (silver/gold checks)
- market overview by symbol (returns and volatility)
- ML baseline metrics and model comparison

Data sources:

- `warehouse/*.duckdb` for analytical queries
- `ml/artifacts/metrics_baseline.json` for ML results
